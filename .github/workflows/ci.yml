name: fakestore-qa CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: write

jobs:
  api-tests:
    name: API Tests (behave)
    runs-on: ubuntu-latest
    env:
      HTTP_PROXY: ${{ secrets.PROXY_URL }}
      HTTPS_PROXY: ${{ secrets.PROXY_URL }}
      NO_PROXY: localhost,127.0.0.1
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Check Fake Store API through proxy
        run: |
          test -n "$HTTPS_PROXY"
          curl --fail --show-error https://fakestoreapi.com/products/1

      - name: Run API tests
        run: |
          behave api_tests/features/ \
            --format allure_behave.formatter:AllureFormatter \
            --outfile allure-results/ \
            --format pretty

      - name: Upload Allure results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: allure-results-api
          path: allure-results/

  ui-tests:
    name: UI Tests (Playwright)
    runs-on: ubuntu-latest
    needs: api-tests
    env:
      HTTP_PROXY: ${{ secrets.PROXY_URL }}
      HTTPS_PROXY: ${{ secrets.PROXY_URL }}
      UI_BASE_URL: http://127.0.0.1:3000
      VITE_API_BASE_URL: http://127.0.0.1:8000
      NO_PROXY: localhost,127.0.0.1
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Install Playwright browsers
        run: playwright install --with-deps chromium

      - name: Start Fake Store API proxy
        run: |
          python .github/scripts/fakestore_proxy.py &
          for i in {1..15}; do
            curl --fail --show-error http://127.0.0.1:8000/products/1 && exit 0
            sleep 1
          done
          exit 1

      - name: Start storefront and wait for ready
        run: docker compose up -d --build --wait

      - name: Check storefront page
        run: |
          curl --fail --show-error http://127.0.0.1:3000
          docker compose logs storefront

      - name: Run UI tests
        run: pytest ui_tests/tests/ --alluredir=allure-results/ -v

      - name: Stop storefront
        if: always()
        run: docker compose down

      - name: Upload Allure results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: allure-results-ui
          path: allure-results/

  publish-report:
    name: Publish Allure Report
    runs-on: ubuntu-latest
    needs: [api-tests, ui-tests]
    if: always() && github.ref == 'refs/heads/main'
    steps:
      - uses: actions/checkout@v4

      - name: Download all Allure results
        uses: actions/download-artifact@v4
        with:
          pattern: allure-results-*
          merge-multiple: true
          path: allure-results/

      - name: Generate Allure report
        uses: simple-elf/allure-report-action@v1.13
        with:
          allure_results: allure-results/
          allure_report: allure-report/
          gh_pages: gh-pages
          allure_history: allure-history

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_branch: gh-pages
          publish_dir: allure-report/
